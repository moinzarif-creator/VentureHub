# VentureHub Updates

## Changes made by Farabi
* Initialized local git repository and set up remote tracking.
* Updated backend models for Core Features (`User`, `Bid`, `Pitch`).
* Updated backend routing (`auth`, `comment`, `bid`, `pitch`).
* Modified frontend pages (`Profile`, `Dashboard`).

These files represent the latest state of the codebase.
